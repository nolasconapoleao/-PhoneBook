//
// Created by nolasco on 24/09/20.
//

#include "Cypher.h"

std::string cypher::decode(const std::string& rawData, const std::string& key) {
    // TODO: Add more sofisticated encryption system
    //  std::string result = "";
    //  const auto cKLen = key.length();
    //  auto k = 0;
    //
    //  for (const auto ch : rawData) {
    //    result += ch - key[k % cKLen - 1];
    //    k++;
    //  }
    //  return result;
    return rawData;
}

std::string cypher::encode(const std::string& rawData, const std::string& key) {
    // TODO: Add more sofisticated encryption system
    //  std::string result = "";
    //  const auto cKLen = key.length();
    //  auto k = 0;
    //
    //  for (const auto ch : rawData) {
    //    result += ch + key[k % cKLen - 1];
    //    k++;
    //  }
    //  return result;
    return rawData;
}